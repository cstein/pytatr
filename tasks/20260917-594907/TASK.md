# sorting of output with tatr ls should be possible by priority

 - STATUS: CLOSED
 - PRIORITY: 100
 - TAGS: feature

There is currently no sorting, but I could / would imagine that it would be beneficial to be able to sort by task id (which is effectively date) or priority.

Actually, we should just do it by adding task id's to a separate list initially.
We can have another list with prioritites, dates and whatever and sort them using standard python machinery.
finally we print.
